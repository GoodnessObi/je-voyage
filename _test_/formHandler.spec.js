import { performAction } from "../src/client/js/formHandler"


describe("Testing the formHandler functionality", () => {  
  test("performAction function exists", () => {
    expect(performAction).toBeDefined();
  })
});